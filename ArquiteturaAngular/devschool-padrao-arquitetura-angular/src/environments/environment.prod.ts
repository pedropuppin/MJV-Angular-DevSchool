export const environment = {
  production: true,
  apiPath: 'http://localhost:3000', // esse link pode ser substituído pelo do servidor real quando for pra produção
} as const

// aruiqvo de configuração do ambiente de produção
